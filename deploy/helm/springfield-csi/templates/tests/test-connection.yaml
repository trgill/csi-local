apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "springfield-csi.fullname" . }}-test-connection"
  labels:
    {{- include "springfield-csi.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "springfield-csi.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
