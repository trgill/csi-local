apiVersion: v1
kind: Service
metadata:
  name: {{ include "springfield-csi.fullname" . }}
  labels:
    {{- include "springfield-csi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "springfield-csi.selectorLabels" . | nindent 4 }}
